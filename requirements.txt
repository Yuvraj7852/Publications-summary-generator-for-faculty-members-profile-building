Flask==3.0.3
Werkzeug==3.0.3

# PDF text extraction (install at least one)
PyMuPDF==1.24.5         # fastest, recommended (pip install PyMuPDF)
pdfplumber==0.11.0      # good fallback
PyPDF2==3.0.1           # basic fallback

# NLP / AI (optional — app works without these using extractive fallback)
transformers==4.41.2
torch==2.3.1            # CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Utilities
python-dotenv==1.0.1
